# ルール：src/ 変更後は必ずビルド

`src/` 配下のファイルを変更した場合は、**必ず** `npm run build` を実行すること。
ビルドが成功するまで「実装完了」と報告しないこと。

## ビルドコマンド

```
npm run build
```

（TypeScript 型チェック + Vite ビルドを実行）

## 対象外

- `README.md` などドキュメントのみの変更はビルド不要
- `src/` 以外のファイル（設定ファイル等）のみの変更はビルド不要
